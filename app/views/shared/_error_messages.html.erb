<%if flash.any? %>
  <div id="error_explanation">
    <!--
    <div class="alert alert-danger">
      The form contains <%= pluralize(flash.count, "error") %>.
    </div>
    -->
    <ul>
    <% flash.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
